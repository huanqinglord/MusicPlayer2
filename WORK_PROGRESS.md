# 工作进度

最后更新：2026-08-14

## 当前目标

以 `MusicPlayer2/skins/01_simple.xml` 极简界面作为默认主界面，继续完善 MusicPlayer2 的 Fluent 风格、播放器布局和“我的分组”功能。

## 已完成

- 将极简界面设为默认界面，保留其他界面原有布局，不再继续改动。
- 移除极简界面左侧的大唱片图片。
- 主内容区调整为左侧歌曲信息、右侧歌词。
- 底部控制区调整为左侧“我的分组”、中央播放控制、右侧音量、循环和播放列表。
- 取消主界面的白色卡片分块，使背景和内容呈现为一个整体。
- 调整播放、上一曲、下一曲以及辅助操作图标的尺寸和间距。
- 将主要操作图标替换为 Microsoft Fluent System Icons，并将图标资源和许可信息纳入项目。
- “我的分组”入口仅显示图标，名称通过鼠标悬停提示显示。
- 取消记录用户自定义主窗口尺寸；每次启动根据屏幕工作区计算合适的默认尺寸。
- 将“我的分组”从独立模态弹窗改为主窗口中的页面式交互。
- 分组页面使用简洁的返回图标，并保留底部进度条和播放控制区。
- 修复打开、关闭播放列表后主界面宽度和布局没有正确恢复的问题。
- 修复“我的分组”页面打开后立即消失、只有滚动条可见以及鼠标悬停时闪烁的问题。
- 将“我的分组”最终调整为由主窗口拥有的无边框覆盖页面，避免与主界面的直接绘制线程互相覆盖。
- 分组页面只覆盖底部控制栏上方的内容区域，并跟随主窗口移动和缩放。
- 修正“我的分组”页面在程序退出时的窗口和对象释放方式。

## 2026-08-14 工作记录

### 极简界面布局

- 确认默认使用极简界面。
- 去掉左侧唱片图片，左侧用于显示标题、艺术家、唱片集和音频格式信息。
- 右侧用于显示歌词；没有歌词时显示对应提示。
- 进度条和播放控制固定在底部，形成桌面音乐播放器常见的信息区、歌词区和控制区结构。
- 去掉三个独立白色面板，让主窗口显示为统一整体。

### 底部按钮与图标

- 左侧放置“我的分组”入口。
- 中央放置上一曲、播放或暂停、下一曲。
- 右侧按音量、循环、播放列表的顺序排列。
- 去掉“我的分组”常驻文字，仅保留文件夹图标和悬停提示。
- 放大普通操作图标和中央播放按钮，提高识别度和视觉平衡。
- 使用 Fluent 风格资源替换原有不统一的 Windows 字体图标。

### 主窗口尺寸

- 取消保存和恢复用户上次调整的窗口尺寸。
- 每次启动根据当前显示器工作区计算默认大小，兼顾不同分辨率，同时保留合理的最小尺寸和窗口边距。

### 我的分组

- 将原有独立弹窗改造成主界面中的页面式内容。
- 使用返回箭头代替传统按钮。
- 页面仅占用播放控制栏上方区域，底部播放按钮和进度条始终可见。
- 处理页面创建、显示、隐藏、返回和退出时的生命周期。
- 针对列表偶发只显示局部、滚动条闪烁的问题，取消子窗口与主界面缓存 DC 的裁剪方案。
- 改用主窗口拥有的无边框覆盖窗口，使列表控件由 Windows 独立合成，同时保持视觉上嵌入主界面。
- 页面位置会在主窗口移动或缩放时同步更新。

## Fluent 图标资源

- Fluent System Icons 上游资源保存在：`E:\MusicPlayer2-downloads\fluentui-system-icons`
- 项目内包含所需 SVG、生成后的 ICO 以及许可说明。
- 图标生成工具：`tools/apply_fluent_icons.py`
- 第三方许可说明：`THIRD-PARTY-NOTICES.md`
- 应用 Logo、文件关联图标和默认专辑封面保持原样。

## 后续继续方式

下次继续前先读取本文件，再根据最新编译结果或界面截图处理剩余的布局和交互问题。
